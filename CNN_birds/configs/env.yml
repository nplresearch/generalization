name: miller_law
channels:
  - pytorch
  - conda-forge
  - defaults
dependencies:
  - python=3.9
  - pytorch=2.1.0
  - torchvision=0.16.0
  - numpy=1.24.3  # Using 1.x version to avoid compatibility issues
  - pandas=2.0.3
  - matplotlib=3.7.1
  - seaborn=0.12.2
  - pyyaml=6.0
  - jupyter=1.0.0
  - ipykernel=6.25.0
  - pip=23.2.1
  - pip:
    - wandb==0.15.8 # optional: for experiment tracking